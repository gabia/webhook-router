{
  "name": "webhook-middleware",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "test": "node --test 'test/**/*.test.js'",
    "deploy": "curl --fail -H \"Authorization: Bearer $COOLIFY_TOKEN\" \"https://coolify.gabia.app/api/v1/deploy?uuid=wj0to771240xcng41x7mmnbo\""
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "cookie-session": "^2.1.1",
    "express": "^5.2.1"
  }
}
